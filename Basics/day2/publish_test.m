%% Some title
% this is an example introduction for testing publish() in MATLAB.

%% Plot something
t = 0:0.01:2*pi;
r = sin(2*t).*cos(2*t);
polar(t,r,'--r');

%%
% we *can* also display LaTeX $e^(i/pi) + 1 = 0$

%% This starts another section title
% This is the _end_ of this document.

%%
% |monospace|
